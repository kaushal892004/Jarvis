# Core functionality
pyaudio==0.2.14                 # Audio input/output for voice control
gTTS==2.5.4                     # Google Text-to-Speech
pygame==2.6.1                   # Optional for sound effects or GUI
speechrecognition==3.10.1      # (Add if not listed) For voice recognition

# Terminal/CLI interface
pyfiglet==1.0.2                 # ASCII banner
colorama==0.4.6                 # Terminal colors

# AI & Gemini integration
google-generativeai==0.8.4     # Gemini model API
google-api-python-client==2.162.0
google-auth==2.38.0
google-auth-httplib2==0.2.0
googleapis-common-protos==1.68.0
proto-plus==1.26.0
protobuf==5.29.3

# Web & search
requests==2.31.0                # (Add if not listed) For web requests (e.g., YouTube, Google search)
openai==0.28.0                  # (Optional) if using OpenAI models

# WhatsApp Messaging
pywhatkit==5.4                  # (Add manually) Used for sending WhatsApp messages

# Email functionality
smtplib                        # Built-in Python module (no need to install)
email-validator==2.1.0         # Email validation

# Extra (Optional/Utility)
beautifulsoup4==4.12.3         # For parsing Google search results
pyautogui==0.9.54              # For controlling screen if needed
pymsgbox==1.0.9                # Simple message boxes
